import React from "react";
import { Text, StyleSheet } from "react-native";

const ItemReviewScreen = () => {
  return <Text style={styles.text}>ItemReviewScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default ItemReviewScreen;
