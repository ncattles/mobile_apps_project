import React from "react";
import { Text, StyleSheet } from "react-native";

const UserReviewScreen = () => {
  return <Text style={styles.text}>UserReviewScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default UserReviewScreen;
