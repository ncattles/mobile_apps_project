import React from "react";
import { Text, StyleSheet } from "react-native";

const SearchScreen = () => {
  return <Text style={styles.text}>SearchScreen</Text>;
};

const styles = StyleSheet.create({
  text: {
    fontSize: 30,
  },
});

export default SearchScreen;
